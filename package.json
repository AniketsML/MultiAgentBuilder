{
  "name": "multi-agent-flow-builder",
  "version": "1.0.0",
  "description": "Multi-Agent AI Conversational Flow Builder",
  "scripts": {
    "start": "concurrently \"python -m uvicorn backend.main:app --reload --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
